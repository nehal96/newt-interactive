import {
  CircuitProteinNode,
  LineNode,
  CircleNode,
  CircuitPromoterNode,
  LabelNode,
} from "@viz/flow";

export const nodeTypes = {
  circle: CircleNode,
  protein: CircuitProteinNode,
  promoter: CircuitPromoterNode,
  line: LineNode,
  label: LabelNode,
};
