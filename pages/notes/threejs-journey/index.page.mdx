import { MdxLayout, PostArticleSubscribe } from "@ui/article";
import { ArticleSection } from "@ui/layout";
import { AnimationsSection, CamerasSection, ShadersCodeSandbox, ShaderPatternsCodeSandbox } from "./figures";
import meta from "./meta";

export const metadata = {
  ...meta,
  subtitle: (
    <>
      Random stuff from Bruno Simon&apos;s course,{" "}
      <a
        href="https://threejs-journey.com/"
        className="text-ink-800 hover:text-ink-900 underline underline-offset-1 decoration-ink-700"
      >
        ThreeJS Journey
      </a>
    </>
  ),
};

### 06 &mdash; Animations

Animations in Three.js work like stop motion &mdash; each time an object moves, you render the changes. Take a look at a few ways you can animate a yellow box below:

<ArticleSection variant="large" className="mt-5 md:mt-4">
  <AnimationsSection />
</ArticleSection>

### 07 &mdash; Cameras

A camera is, as you can imagine, what you use to see the scene in Three.js.

<ArticleSection variant="large" className="mt-5 md:mt-4">
  <CamerasSection />
</ArticleSection>

### 28 &mdash; Shaders

<ArticleSection variant="large" className="mt-5 md:mt-4">
  <ShadersCodeSandbox />
</ArticleSection>


### 29 &mdash; Shader patterns

<ArticleSection variant="large" className="mt-5 md:mt-4">
  <ShaderPatternsCodeSandbox />
</ArticleSection>

<PostArticleSubscribe />

export default function MDXPage({ children }) {
  return (
    <MdxLayout metadata={metadata}>{children}</MdxLayout>
  );
}