// Where to reach the author. Written once: the footer and the two "get in
// touch" paragraphs (the homepage's and the one at the foot of every article)
// all point here, so changing a handle or an address is one edit rather than
// three that have to be found.

export const TWITTER_URL = "https://www.twitter.com/nehaludyavar";

export const EMAIL_HREF = `mailto:nehaludyavar@gmail.com?subject=${encodeURIComponent(
  "Hello"
)}`;
