// Homepage is deliberately absent — it pulls CoverArt and the whole catalogue.
// Its two pages deep-import it.

export { default as Footer } from "./Footer";
export { default as Navbar } from "./Navbar";
